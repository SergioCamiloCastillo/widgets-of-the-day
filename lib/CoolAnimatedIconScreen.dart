import 'package:flutter/material.dart';

class CoolAnimatedIconScreen extends StatelessWidget {
  const CoolAnimatedIconScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
